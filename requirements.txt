# Web framework and server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# MongoDB async driver
motor==3.4.0
pymongo==4.7.2

# Authentication
python-jose[cryptography]==3.3.0
bcrypt==4.2.1          # used directly - passlib removed (breaks on Python 3.14)

# Environment variables
python-dotenv==1.0.1

# Gemini LLM — using REST API directly (no SDK) for Python 3.14 compatibility.
# google-generativeai depends on protobuf's C extension which crashes on 3.14.
# httpx (already listed below) handles the REST calls instead.

# HTTP (Gemini + Streamlit → FastAPI calls + async weather fetch)
# requests is no longer used by weather.py (converted to async httpx)
# kept here in case any transitive dependency needs it
httpx==0.27.0

# Frontend
streamlit==1.35.0
extra-streamlit-components==0.1.71   # CookieManager — persists JWT across page refreshes

# Testing
pytest
pytest-asyncio      # required for async test functions in test_weather.py
